export declare class LocalDTO {
    id?: number;
    capacidade: number;
    local: string;
    descricao: string;
    constructor(capacidade: number, local: string, descricao: string, id: number);
}
